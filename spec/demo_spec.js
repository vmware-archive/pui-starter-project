var TileLayout = require('../js/tileLayout').TileLayout;

describe('TileLayout', () => {
  it('creates tile-layout')
  it('creates tile-items')
  describe('columns', () => {
    it('adds classes')
  });
  describe('gutters', () => {
    it('adds class')
  })
});
